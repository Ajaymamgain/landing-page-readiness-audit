{
  "name": "landing-page-readiness-audit",
  "version": "1.1.0",
  "private": true,
  "description": "Stock Lighthouse + Playwright + optional CRO → one readiness-report.html for paid landing pages.",
  "type": "module",
  "scripts": {
    "lh": "node scripts/lighthouse-run.mjs",
    "probe": "node scripts/playwright-probe.mjs",
    "cro": "node scripts/cro-audit-run.mjs",
    "report": "node scripts/run-report.mjs"
  },
  "devDependencies": {
    "lighthouse": "^12.8.1",
    "playwright": "^1.52.0"
  },
  "engines": {
    "node": ">=20"
  }
}
