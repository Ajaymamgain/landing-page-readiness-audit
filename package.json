{
  "name": "google-ads-readiness-audit",
  "version": "1.1.0",
  "private": true,
  "description": "Stock Lighthouse + Playwright → ONE readiness-report.html (no publisher-ads plugin).",
  "type": "module",
  "scripts": {
    "lh": "node scripts/lighthouse-run.mjs",
    "probe": "node scripts/playwright-probe.mjs",
    "report": "node scripts/run-report.mjs",
    "audit": "node scripts/run-report.mjs"
  },
  "devDependencies": {
    "lighthouse": "^12.8.1",
    "playwright": "^1.52.0"
  },
  "engines": {
    "node": ">=20"
  }
}
